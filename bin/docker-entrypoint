#!/bin/bash -e

# Create or migrate the database on every boot. Local-first means the SQLite DB
# lives in the mounted data dir, so any entrypoint command (server, rake tasks
# like scribe:ingest, console) must run against an up-to-date schema.
./bin/rails db:prepare

exec "${@}"
